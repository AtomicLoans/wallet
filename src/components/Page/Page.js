import React, {useEffect, useState} from 'react';
import {View} from 'react-native';

const Page = ({children}) => {
  return <View>{children}</View>;
};

export const TopContainer = View,
  BottomContainer = View;

export default Page;
